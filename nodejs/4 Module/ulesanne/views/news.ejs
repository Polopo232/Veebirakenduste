<!DOCTYPE html>
<html lang="et">
<head>
    <meta charset="UTF-8">
    <title>Uudis | <%= news.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
    <style>
        .news-card {
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            border-radius: 10px;
            transition: transform 0.2s, box-shadow 0.2s;
            padding: 2rem;
            background: #ffffff;
        }
        .news-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.2);
        }
        .news-title {
            font-weight: 700;
            font-size: 2rem;
            margin-bottom: 0.5rem;
        }
        .news-date {
            color: #888;
            font-size: 0.9rem;
            margin-bottom: 1.5rem;
        }
        .news-content {
            font-size: 1.1rem;
            line-height: 1.6;
            margin-bottom: 2rem;
        }
        .news-buttons a {
            margin-right: 0.5rem;
        }
    </style>
</head>
<body>
<%- include('partials/header') %>


<div class="container mt-5">
    <div class="news-card">
        <div class="d-flex justify-content-between align-items-start mb-3">
            <div>
                <h1 class="news-title"><%= news.title %></h1>
                <div class="news-date">
                    <%= new Date(news.created_at).toLocaleDateString('et-EE', { year: 'numeric', month: 'long', day: 'numeric' }) %>
                </div>
            </div>
        </div>

        <p class="news-content"><%= news.content %></p>

        <div class="d-flex justify-content-between my-3">
            <a href="/" class="btn btn-danger">Tagasi avalehele</a>
            <a href="/news/<%= news.id %>/edit" class="btn btn-info">Muuda uudist</a>
        </div>
    </div>
</div>

</body>
</html>
